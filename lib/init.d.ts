/**
 * @summary Init the fields process
 * @description Combine all the fields and groups into one array, add a real id and write it to a file fields.json
 * @param {Array<T.COMMON_FIELDS & T.COMMON_GROUP_FIELDS>} childrens
 * @returns undefined
 */
export function init(...childrens: Array<T.COMMON_FIELDS & T.COMMON_GROUP_FIELDS>): Promise<void>;
import * as T from "./config/types.js";
//# sourceMappingURL=init.d.ts.map